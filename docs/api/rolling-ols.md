# RollingOLS

The main model class. Construct with parameters, call `.fit()` to register
regressors, then `.transform()` to produce results.

::: rols.RollingOLS
    options:
      members:
        - __init__
        - fit
        - transform
        - fit_transform
        - estimate_memory
